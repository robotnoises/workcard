export type CardProps = {
  name: string,
  title: string,
  location: string,
  employer: string,
};

const CardPage = (props: CardProps) => {
  return (
    <p>hey</p>
  );
};

export default CardPage;
